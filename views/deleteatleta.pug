<!DOCTYPE html>
html(lang="es-ES")
    head
        title= title
    body
        h1 Inscribir atleta
        form(action="/api/club/1913" method="delete")
            input(type="text" name="codigo" placeholder="Código del club" required)
            br
            input(type="text" name="id" placeholder="Id del atleta" required)
            br   
            input(type="submit")
